public class QueueExample implements Queueable{
    public void execute (QueueableContext context) {
        // my code
        Account acc = new Account(Name = 'Clarusway');
        Insert acc;

    }
}

// Apex code: System.enqueueJob(new QueueExample());
//check in UI as Apex Jobs